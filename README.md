# Alphard Tracker

A single-page React app for tracking Toyota Alphard 20-series import candidates.

## Usage

Open `index.html` in a browser or visit the GitHub Pages deployment.

Car data lives in `data/cars.json`. Photos go in `images/carXX/` folders.

## Deployment

Hosted via GitHub Pages — deploy from the `main` branch.
